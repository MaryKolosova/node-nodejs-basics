const compress = async () => {
    // Write your code here
};

await compress();

import { createGzip } from 'zlib';

const oldPath = "./src/zip/files/fileToCompress.txt";
const newPath = "./src/zipfs/files/archive.gz";